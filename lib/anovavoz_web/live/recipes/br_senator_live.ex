defmodule AnovavozWeb.BrSenatorLive do
  use AnovavozWeb, :live_view
  use AnovavozNative, :live_view

  def mount(_params, _session, socket) do
    {:ok, socket}
  end

  def render(assigns) do
    ~H""
  end
end
